import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.evilcorp.QRGenerator',
  appName: 'Ionic QR Generator',
  webDir: 'www'
};

export default config;
